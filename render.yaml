services:
  - type: web
    name: nexus-trusai-backend
    env: python
    rootDir: trus-ai-mvp/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.13
