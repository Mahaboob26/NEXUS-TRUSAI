services:
  - type: web
    name: nexus-trusai-monolith
    env: docker
    plan: free
    region: ohio
    # No buildCommand needed for Docker
    # No startCommand needed for Docker (uses CMD from Dockerfile)
    envVars:
      - key: BACKEND_URL
        value: http://127.0.0.1:8000
